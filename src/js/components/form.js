/**
 * Form 
 *
 * @file form.js
 */

const form = (function () {
    const init = function () {
        formSubmitControl();
    };

    const formSubmitControl = () => {
     
      };

    return {
        init,
    };
}());

export default form;
